"""
Shared configuration constants and defaults.

This module lives under the config/ directory (not the config package).
Import from the root config module when helpers are implemented:

    from config import load_constants  # future
"""

# Placeholder — module-specific constants will be defined here.
